spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: 43.139.66.63:8848
      discovery:
        server-addr: ${spring.cloud.nacos.server-addr}
        namespace: note
        group: DEFAULT_GROUP
      config:
        server-addr: ${spring.cloud.nacos.server-addr}
        namespace: note
        group: DEFAULT_GROUP
        file-extension: yml
        shared-configs:
          - dataId: datasource.yml
            group: DEFAULT_GROUP
            refresh: true